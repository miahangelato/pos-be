<!DOCTYPE html>
<html>
<head>
  <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  <style>
    body {
      font-family: Arial, sans-serif;
      line-height: 1.6;
      color: #333;
      max-width: 600px;
      margin: 0 auto;
      padding: 20px;
    }
    .header {
      background-color: #a855f7;
      color: white;
      padding: 20px;
      text-align: center;
      border-radius: 8px 8px 0 0;
    }
    .content {
      background-color: #f9fafb;
      padding: 30px;
      border: 1px solid #e5e7eb;
    }
    .section {
      background-color: white;
      padding: 20px;
      margin-bottom: 20px;
      border-radius: 8px;
      border: 1px solid #e5e7eb;
    }
    .gcash-box {
      background: linear-gradient(135deg, #a855f7 0%, #ec4899 100%);
      color: white;
      padding: 20px;
      border-radius: 8px;
      margin-bottom: 20px;
      text-align: center;
    }
    .gcash-box h3 {
      margin: 0 0 10px 0;
      font-size: 18px;
    }
    .gcash-box .amount {
      font-size: 32px;
      font-weight: bold;
      margin: 15px 0;
    }
    .cta-button {
      display: inline-block;
      background-color: #a855f7;
      color: white;
      padding: 15px 30px;
      text-decoration: none;
      border-radius: 8px;
      font-weight: 600;
      margin: 15px 0;
      text-align: center;
    }
    .cta-button:hover {
      background-color: #9333ea;
    }
    .instructions {
      background-color: #fef3c7;
      border-left: 4px solid #f59e0b;
      padding: 15px;
      margin-bottom: 20px;
    }
    .instructions h4 {
      color: #92400e;
      margin-top: 0;
    }
    .instructions li {
      margin-bottom: 8px;
    }
    .footer {
      text-align: center;
      padding: 20px;
      color: #6b7280;
      font-size: 14px;
    }
    table {
      width: 100%;
      border-collapse: collapse;
      margin-bottom: 20px;
    }
    th {
      background-color: #f3f4f6;
      padding: 12px;
      text-align: left;
      font-weight: 600;
      border-bottom: 2px solid #e5e7eb;
    }
    td {
      padding: 12px;
      border-bottom: 1px solid #e5e7eb;
    }
  </style>
</head>
<body>
  <div class="header">
    <h1>💜 GCash Payment Required</h1>
  </div>

  <div class="content">
    <!-- GCash Amount Box -->
    <div class="gcash-box">
      <h3>Please transfer the following amount via GCash:</h3>
      <div class="amount">₱<%= number_with_precision(@order.grand_total, precision: 2) %></div>
      <p>Order Reference: <strong><%= @order.reference_number %></strong></p>
    </div>

    <!-- Main Message -->
    <div class="section">
      <h2>Hello <%= @order.customer.full_name %>,</h2>
      <p>Thank you for placing your order! We've received your order and are waiting for your GCash payment.</p>
      <p>Once you've completed the GCash transfer, please upload a screenshot of your transaction confirmation below.</p>
    </div>

    <!-- Instructions -->
    <div class="instructions">
      <h4>📝 GCash Transfer Instructions:</h4>
      <ol>
        <li>Open your GCash app</li>
        <li>Go to "Send Money" and select the merchant account</li>
        <li>Enter the amount: <strong>₱<%= number_with_precision(@order.grand_total, precision: 2) %></strong></li>
        <li>Complete the transfer (save your reference number!)</li>
        <li>Take a screenshot showing the transaction confirmation</li>
      </ol>
    </div>

    <!-- Order Details -->
    <div class="section">
      <h3>Order Details</h3>
      <table>
        <thead>
          <tr>
            <th>Product</th>
            <th>Quantity</th>
            <th>Price</th>
            <th>Total</th>
          </tr>
        </thead>
        <tbody>
          <% @order.order_items.each do |item| %>
            <tr>
              <td><%= item.product.name %></td>
              <td><%= item.quantity %></td>
              <td>₱<%= number_with_precision(item.price_at_purchase, precision: 2) %></td>
              <td>₱<%= number_with_precision(item.line_total, precision: 2) %></td>
            </tr>
          <% end %>
        </tbody>
      </table>

      <div style="background-color: #f3f4f6; padding: 15px; border-radius: 8px;">
        <div style="display: flex; justify-content: space-between; margin-bottom: 10px;">
          <span>Subtotal:</span>
          <strong>₱<%= number_with_precision(@order.subtotal, precision: 2) %></strong>
        </div>
        <% if @order.shipping_fee.to_f > 0 %>
          <div style="display: flex; justify-content: space-between; margin-bottom: 10px;">
            <span>Shipping Fee:</span>
            <strong>₱<%= number_with_precision(@order.shipping_fee, precision: 2) %></strong>
          </div>
        <% end %>
        <% if @order.convenience_fee.to_f > 0 %>
          <div style="display: flex; justify-content: space-between; margin-bottom: 10px;">
            <span>Convenience Fee:</span>
            <strong>₱<%= number_with_precision(@order.convenience_fee, precision: 2) %></strong>
          </div>
        <% end %>
        <div style="display: flex; justify-content: space-between; border-top: 2px solid #d1d5db; padding-top: 10px;">
          <strong>TOTAL TO PAY:</strong>
          <strong style="color: #a855f7; font-size: 18px;">₱<%= number_with_precision(@order.grand_total, precision: 2) %></strong>
        </div>
      </div>
    </div>

    <!-- Upload Proof Section -->
    <div class="section" style="background-color: #f0f9ff;">
      <h3 style="color: #0369a1;">📸 Upload Payment Proof</h3>
      <p>Click the button below to upload your GCash transaction screenshot:</p>
      <div style="text-align: center;">
        <a href="<%= new_payment_proof_url(order_id: @order.id) %>" class="cta-button">
          🔗 Upload Payment Proof
        </a>
      </div>
      <p style="text-align: center; color: #6b7280; font-size: 12px; margin-top: 15px;">
        Or copy this link: <%= new_payment_proof_url(order_id: @order.id) %>
      </p>
    </div>

    <!-- What Happens Next -->
    <div class="section">
      <h3>📋 What Happens Next?</h3>
      <ol>
        <li><strong>Upload Screenshot:</strong> Submit your GCash transaction screenshot</li>
        <li><strong>Verification:</strong> Our team will verify your payment (usually within 24 hours)</li>
        <li><strong>Confirmation:</strong> You'll receive an email once payment is verified</li>
        <li><strong>Shipment:</strong> Your order will be processed for shipment immediately after verification</li>
      </ol>
    </div>

    <!-- Important Notes -->
    <div style="background-color: #fee2e2; border-left: 4px solid #ef4444; padding: 15px; border-radius: 8px; margin-bottom: 20px;">
      <h4 style="color: #991b1b; margin-top: 0;">⚠️ Important:</h4>
      <ul style="margin: 10px 0; padding-left: 20px;">
        <li>Ensure your GCash transaction reference number is clearly visible in the screenshot</li>
        <li>Include the transaction date and amount in your screenshot</li>
        <li>Payment proof must be submitted within 24 hours to avoid order cancellation</li>
        <li>If you have any issues, please contact our support team</li>
      </ul>
    </div>

    <!-- Support -->
    <div class="section">
      <h3>Need Help?</h3>
      <p>If you have any questions or issues with uploading your payment proof, please contact us:</p>
      <p><strong>Email:</strong> support@posystem.com</p>
      <p><strong>Contact Number:</strong> +63 (2) 1234-5678</p>
    </div>

    <!-- Footer -->
    <div class="footer">
      <p>Thank you for your business! We appreciate your order.</p>
      <p style="margin-top: 10px; font-size: 12px; color: #9ca3af;">
        This is an automated email. Please do not reply directly. Contact support if you have questions.
      </p>
    </div>
  </div>
</body>
</html>
